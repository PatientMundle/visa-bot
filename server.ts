import { startBot } from './src/lib/bot';`n`nstartBot();`n`nconst port = process.env.PORT || 3000;`nBun.serve({`n  port: Number(port),`n  fetch() { return new Response('Bot running'); }`n});`nconsole.log('Server running on port ' + port);
